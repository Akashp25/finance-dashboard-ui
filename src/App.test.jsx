import React from 'react';

// Minimal test component to verify React is working
function TestApp() {
  return (
    <div style={{ padding: '20px', backgroundColor: 'lightblue' }}>
      <h1 style={{ color: 'red', fontSize: '24px' }}>Test App Working!</h1>
      <p>If you can see this, React is working but there's an issue with the main app.</p>
    </div>
  );
}

export default TestApp;
